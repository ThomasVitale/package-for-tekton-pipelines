---
apiVersion: v1
kind: Secret
metadata:
  name: tekton-pipelines-values
  namespace: tests
stringData:
  values.yaml: |
    controllers:
      pipelines:
        replicas: 3
      resolvers:
        replicas: 3

    leader_election:
      lease_duration: "60s"
      renew_deadline: "40s"
      retry_period: "10s"
      buckets: "3"

    webhook:
      minReplicas: 2
