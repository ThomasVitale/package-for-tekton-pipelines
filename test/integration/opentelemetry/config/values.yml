---
apiVersion: v1
kind: Secret
metadata:
  name: tekton-pipelines-values
  namespace: tests
stringData:
  values.yaml: |
    opentelemetry:
      enable: true
      exporter:
        jaeger:
          endpoint: "http://tempo.observability-stack:14268/api/traces"
          username: jon
          password: snow
